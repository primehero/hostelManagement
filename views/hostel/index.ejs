<% include ../partials/header %>

<% include ../partials/sidebar %>

<div class="col-md-9">
  <table class="table">
    <thead>
      <tr>
        <th>Category Name</th>
        <th>Details</th>
      </tr>
    </thead>
    <tbody>
      <% hostels.forEach(function(hostel) { %>
      <tr>
        <td>
          <%= hostel.name %>
        </td>
        <!-- SHOW -->
        <td>
          <a class="btn btn-primary" href="/hostel/<%= hostel._id %>">
            Details
          </a>
        </td>
      </tr>
      <% }) %>
    </tbody>
  </table>
</div><!-- ./col -->


<% include ../partials/footer %>
